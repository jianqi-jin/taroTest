<block wx:if="{{$taroCompReady}}">
    <view>
        <view>{{count}}</view>
        <button bindtap="onClick" data-e-tap-so="this" data-e-tap-a-a="add">+1</button>
        <button bindtap="onClick" data-e-tap-so="this" data-e-tap-a-a="reduce">-1</button>
        <theme-view>
            <view class="themeText">Im ThemeView Text !</view>
        </theme-view>
    </view>
</block>